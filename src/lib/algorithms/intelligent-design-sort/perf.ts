/**
 * Intelligent Design Sort - Performance implementation.
 * The array is already in it's perfect, Sorter-approved order.
 *
 * @param {number[]} arr The input array.
 * @returns {number[]} The same, already sorted array.
 */
export default function intelligentDesignSort(arr: number[]): number[] {
	return arr;
}
